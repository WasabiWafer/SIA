#pragma once

// #pragma comment(lib, "Kernel32")
// #include <Windows.h>

#include "SIA/internals/types.hpp"

namespace sia
{
    // size_t get_thread_id() noexcept { return GetCurrentThreadId(); }
    // size_t get_thread_id(HWND handle) { return GetThreadId(handle); }
} // namespace sia
